# Allo Reservation App Instructions

- Use the App Router, TypeScript, Prisma, and hosted PostgreSQL for the reservation workflow.
- Keep reservation logic transaction-safe and preserve the row-locking behavior in `lib/inventory.ts`.
- Prefer concise, product-focused UI changes that keep stock counts and reservation state obvious.
- Update the README when deployment, expiry, or idempotency behavior changes.